# 🚀 Покрокова інструкція - Що робити зараз

## Крок 1: Ініціалізація Git репозиторію (якщо ще не зроблено)

Відкрий термінал і виконай:

```bash
cd /home/dchuprina/skrynia
git init
git branch -M main
```

## Крок 2: Додай remote репозиторій

Якщо у тебе вже є репозиторій на GitHub:

```bash
git remote add origin https://github.com/ТВІЙ_USERNAME/skrynia.git
```

Заміни `ТВІЙ_USERNAME` на свій GitHub username.

Якщо репозиторію ще немає:
1. Перейди на https://github.com
2. Натисни **New repository**
3. Назва: `skrynia`
4. Виберіть **Private** (рекомендовано)
5. НЕ додавай README, .gitignore, license
6. Натисни **Create repository**
7. Скопіюй URL репозиторію
8. Виконай команду вище з твоїм URL

## Крок 3: Перевір що всі файли на місці

```bash
# Перевір workflow файли
ls -la .github/workflows/

# Маєш побачити:
# - ci-cd.yml
# - deploy.yml
# - docker-image.yml
# - lint.yml
# - security.yml
```

## Крок 4: Додай всі файли до git

```bash
git add .
```

## Крок 5: Зроби перший коміт

```bash
git commit -m "Initial commit: Setup CI/CD з GitHub Actions, 2FA, security headers"
```

## Крок 6: Push до GitHub

```bash
git push -u origin main
```

Якщо це перший раз, GitHub може попросити авторизацію.

## Крок 7: Налаштування Secrets в GitHub

### 7.1. Відкрий репозиторій на GitHub
Перейди на: `https://github.com/ТВІЙ_USERNAME/skrynia`

### 7.2. Відкрий Settings
Натисни **Settings** (вгорі репозиторію)

### 7.3. Відкрий Secrets
В лівому меню знайди: **Secrets and variables** → **Actions**

### 7.4. Додай перший Secret
1. Натисни **New repository secret**
2. **Name:** `NEXT_PUBLIC_API_URL`
3. **Secret:** `https://api.runebox.eu` (або твій URL)
4. Натисни **Add secret**

### 7.5. Додай другий Secret
1. Натисни **New repository secret** знову
2. **Name:** `NEXT_PUBLIC_SITE_URL`
3. **Secret:** `https://runebox.eu` (або твій URL)
4. Натисни **Add secret**

## Крок 8: Перевірка GitHub Actions

### 8.1. Відкрий вкладку Actions
В репозиторії натисни **Actions** (вгорі)

### 8.2. Перевір workflow
Ти побачиш список workflow. Вони можуть бути в стані "running" або "completed"

### 8.3. Клікни на будь-який workflow
Ти побачиш деталі виконання. Якщо щось не працює - подивись логи.

## Крок 9: Перевірка результатів

### 9.1. Перевір Packages
1. В репозиторії натисни **Packages** (праворуч вгорі)
2. Після першого build ти побачиш:
   - `backend:latest`
   - `frontend:latest`

### 9.2. Перевір Security
1. Натисни **Security** (вгорі репозиторію)
2. Перевір чи немає критичних проблем

## Що робити далі?

### Якщо все працює ✅:
1. Перевір логи workflow - чи все пройшло успішно
2. Перевір образи Docker в Packages
3. Можеш використовувати образи для deployment

### Якщо щось не працює ❌:
1. Відкрий failed workflow
2. Подивись логи - там буде помилка
3. Перевір чи всі secrets додані
4. Перевір чи Dockerfile правильний

## Важливі команди для перевірки:

```bash
# Перевір статус git
git status

# Перевір remote
git remote -v

# Перевір гілки
git branch

# Перевір останні коміти
git log --oneline -5
```

## Допомога:

Якщо щось не працює, перевір:
- `.github/SETUP.md` - детальна інструкція
- `.github/QUICKSTART.md` - швидкий старт
- `ІНСТРУКЦІЯ_ДЕПЛОЙ.md` - покрокова інструкція

