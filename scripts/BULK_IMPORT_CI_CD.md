# Bulk Import через CI/CD - Швидкий Старт

## ✅ Що налаштовано:

1. **GitHub Actions Workflow** (`.github/workflows/bulk-import-products.yml`)
   - Автоматичний запуск при зміні файлів в `товари/`
   - Manual запуск через GitHub UI
   - Опціональний schedule (щодня о 3:00 UTC)

2. **Інтеграція в Deploy** (`.github/workflows/deploy.yml`)
   - Автоматичний bulk import після deployment на production
   - Запускається тільки на `main` branch

## 🚀 Як використовувати:

### Варіант 1: Автоматично при push

```bash
# Додайте товари в папку товари/
git add товари/
git commit -m "Додано нові товари"
git push origin main
```

Workflow автоматично:
1. Запуститься при зміні файлів в `товари/`
2. Завантажить всі товари на сервер
3. Покаже результат в Actions

### Варіант 2: Manual запуск

1. Перейдіть до **GitHub** → **Actions**
2. Виберіть **Bulk Import Products**
3. Клікніть **Run workflow**
4. Виберіть branch (`main`)
5. Налаштуйте параметри (опціонально):
   - `products_path`: `товари` (за замовчуванням)
   - `api_url`: URL вашого API
   - `skip_errors`: `false` (пропустити помилки)
6. Клікніть **Run workflow**

### Варіант 3: Автоматично після deployment

При push в `main`:
1. Запускається `deploy.yml`
2. Після успішного deployment автоматично запускається bulk import
3. Товари завантажуються на production

## 🔐 Secrets (обов'язково):

Встановіть в **GitHub** → **Settings** → **Secrets and variables** → **Actions**:

```
ADMIN_EMAIL = ваш@email.com
ADMIN_PASSWORD = ваш_пароль
BACKEND_URL = https://api.runebox.eu
```

Або використовується `NEXT_PUBLIC_API_URL` якщо `BACKEND_URL` не встановлено.

## 📁 Структура:

```
товари/
└── product-slug/
    ├── product.json          ← Обов'язково
    ├── description-uk.md     ← Опціонально
    ├── image-1.jpg           ← Зображення
    └── video.mp4             ← Відео
```

## 📊 Результати:

Після виконання:
- Перейдіть до **Actions** → виберіть workflow run
- Перегляньте логи кожного кроку
- Подивіться summary в кінці

## ⚠️ Важливо:

- ✅ Bulk import **не блокує** deployment при помилках
- ✅ Автоматично визначає, чи товар вже існує (за slug)
- ✅ Оновлює існуючі товари або створює нові
- ✅ Завантажує всі зображення та відео
- ✅ Підтримує `.md` файли для описів

## 🔍 Troubleshooting:

**Помилка: "Не вдалося отримати token"**
- Перевірте `ADMIN_EMAIL` та `ADMIN_PASSWORD` в Secrets

**Помилка: "Папка не існує"**
- Переконайтеся, що папка `товари/` існує в репозиторії

**API не готовий**
- Workflow чекає до 60 секунд на готовність API
- Можна запустити bulk import вручну пізніше

## 📚 Детальна документація:

- **Workflow**: `.github/workflows/README_BULK_IMPORT.md`
- **Скрипт**: `scripts/README_BULK_IMPORT.md`
- **Формат даних**: `BULK_IMPORT_GUIDE.md`

