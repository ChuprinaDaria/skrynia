# Інструкція: Bulk Import Товарів

## Швидкий Старт

### 1. Отримайте Admin Token

Спочатку потрібно отримати токен адміністратора:

```bash
# Вхід через API
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "your@email.com", "password": "your_password"}'
```

Скопіюйте `access_token` з відповіді.

### 2. Встановіть змінні середовища

```bash
export ADMIN_TOKEN="ваш_токен_тут"
export API_BASE_URL="http://localhost:8000"  # або URL вашого сервера
```

### 3. Запустіть скрипт

```bash
# З кореня проекту
python scripts/bulk_import_products.py
```

Або з вказанням параметрів:

```bash
python scripts/bulk_import_products.py \
  --path товари/ \
  --api-url http://localhost:8000 \
  --token ваш_токен
```

## Що робить скрипт:

1. ✅ Знаходить всі папки з `product.json` в папці `товари/`
2. ✅ Читає дані з `product.json`
3. ✅ Завантажує `.md` файли якщо вказано `*_file` поля
4. ✅ Завантажує всі зображення на сервер
5. ✅ Завантажує всі відео на сервер
6. ✅ Створює або оновлює товар через API
7. ✅ Показує підсумок операції

## Структура папки товару:

```
товари/
└── coral-brass-necklace-with-bells/
    ├── product.json              ← Обов'язково
    ├── description-uk.md          ← Опціонально
    ├── description-en.md          ← Опціонально
    ├── legend-content-uk.md      ← Опціонально
    ├── photo_1.jpg               ← Зображення
    ├── photo_2.jpg
    └── video.mp4                  ← Відео (опціонально)
```

## Приклад використання:

```bash
# 1. Отримайте токен
TOKEN=$(curl -s -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@example.com","password":"password"}' \
  | jq -r '.access_token')

# 2. Запустіть імпорт
python scripts/bulk_import_products.py --token "$TOKEN"
```

## Вихід скрипта:

```
📦 Знайдено 1 товарів для завантаження

[1/1] Обробка: coral-brass-necklace-with-bells
    📷 Завантаження зображення photo_1.jpg...
    📷 Завантаження зображення photo_2.jpg...
  ✅ СТВОРЕНО: coral-brass-necklace-with-bells
     Зображень: 2, Відео: 0

============================================================
📊 Підсумок:
   ✅ Успішно: 1
   ❌ Помилок: 0
============================================================
```

## Помилки та вирішення:

### Помилка: "Не вказано ADMIN_TOKEN"
```bash
export ADMIN_TOKEN="ваш_токен"
```

### Помилка: "Папка не існує"
Перевірте, що папка `товари/` існує в корені проекту.

### Помилка: "product.json не знайдено"
Переконайтеся, що в кожній підпапці є файл `product.json`.

### Помилка завантаження зображень
- Перевірте формат файлів (JPG, PNG, WebP)
- Перевірте розмір файлів (максимум 10MB)
- Перевірте права доступу до папки uploads на сервері

## Автоматизація:

Можна створити скрипт-обгортку:

```bash
#!/bin/bash
# import.sh

TOKEN=$(curl -s -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@example.com","password":"password"}' \
  | jq -r '.access_token')

python scripts/bulk_import_products.py --token "$TOKEN"
```

## Примітки:

- Скрипт автоматично визначає, чи товар вже існує (за slug)
- Якщо товар існує - він буде оновлено
- Якщо товар не існує - він буде створено
- Зображення завантажуються автоматично
- Перше зображення стає головним

