# 📋 Покрокова інструкція для деплою

## Крок 1: Перевірка файлів

Переконайся, що всі файли на місці:

```bash
cd /home/dchuprina/skrynia
ls -la .github/workflows/
```

Маєш побачити:
- ✅ ci-cd.yml
- ✅ deploy.yml
- ✅ docker-image.yml
- ✅ lint.yml
- ✅ security.yml

## Крок 2: Налаштування GitHub Secrets

### 2.1. Відкрий GitHub репозиторій
1. Перейди на https://github.com
2. Відкрий свій репозиторій `skrynia`

### 2.2. Додай Secrets
1. Натисни **Settings** (вгорі репозиторію)
2. В лівому меню знайди **Secrets and variables** → **Actions**
3. Натисни **New repository secret**

### 2.3. Додай перший secret:
- **Name:** `NEXT_PUBLIC_API_URL`
- **Secret:** `https://api.runebox.eu` (або твій URL API)
- Натисни **Add secret**

### 2.4. Додай другий secret:
- **Name:** `NEXT_PUBLIC_SITE_URL`
- **Secret:** `https://runebox.eu` (або твій URL сайту)
- Натисни **Add secret**

## Крок 3: Перевірка Dockerfile

Переконайся, що Dockerfile існують:

```bash
ls -la skrynia/Dockerfile
ls -la skrynia/backend/Dockerfile
```

Обидва файли мають існувати.

## Крок 4: Коміт і Push

### 4.1. Перевір статус git:
```bash
git status
```

### 4.2. Додай всі зміни:
```bash
git add .
```

### 4.3. Зроби коміт:
```bash
git commit -m "Налаштування GitHub Actions CI/CD з 2FA та security"
```

### 4.4. Push до GitHub:
```bash
git push origin main
```

Якщо у тебе інша назва гілки (не main), заміни:
```bash
git push origin твоя_гілка
```

## Крок 5: Перевірка GitHub Actions

### 5.1. Відкрий GitHub репозиторій
1. Перейди на https://github.com/твій_username/skrynia
2. Натисни вкладку **Actions** (вгорі)

### 5.2. Перевір workflow
Ти побачиш список workflow:
- **CI/CD Pipeline** - головний pipeline
- **Docker Image CI** - build Docker образів
- **Security Scan** - перевірка безпеки
- **Lint Code** - перевірка коду
- **Deploy to Production** - деплой

### 5.3. Клікни на будь-який workflow
Ти побачиш деталі виконання. Якщо щось не працює - подивись логи.

## Крок 6: Перевірка результатів

### 6.1. Перевір Packages (Docker образи)
1. В репозиторії натисни **Packages** (праворуч вгорі)
2. Ти побачиш образи:
   - `backend:latest`
   - `frontend:latest`

### 6.2. Перевір Security
1. Натисни **Security** (вгорі репозиторію)
2. Перевір чи немає критичних проблем

## Крок 7: Перший деплой (якщо потрібно)

Якщо хочеш задеплоїти на сервер:

### 7.1. Додай SSH secrets (якщо потрібно)
В **Settings → Secrets → Actions** додай:
- `SSH_HOST` - IP або домен сервера
- `SSH_USER` - користувач SSH
- `SSH_PRIVATE_KEY` - приватний ключ SSH

### 7.2. Розкоментуй deployment в workflow
Відредагуй `.github/workflows/ci-cd.yml` та додай свої кроки deployment.

## Що робити якщо щось не працює?

### Проблема: Workflow не запускається
**Рішення:**
1. Перевір чи Actions увімкнені: Settings → Actions → General
2. Перевір чи є гілка `main`
3. Перевір чи файли `.github/workflows/*.yml` є в репозиторії

### Проблема: Build fails
**Рішення:**
1. Відкрий failed workflow в Actions
2. Подивись логи - там буде помилка
3. Перевір чи всі secrets додані
4. Перевір чи Dockerfile правильний

### Проблема: Secrets не працюють
**Рішення:**
1. Перевір чи назви secrets точно такі ж як в workflow
2. Перевір чи secrets додані в Settings → Secrets
3. Пам'ятай що secrets case-sensitive

### Проблема: Docker build fails
**Рішення:**
1. Перевір чи Dockerfile існує
2. Перевір чи context path правильний
3. Подивись логи build в Actions

## Чеклист перед першим push:

- [ ] Всі файли workflow додані в `.github/workflows/`
- [ ] Secrets додані в GitHub Settings
- [ ] Dockerfile існують для backend і frontend
- [ ] .dockerignore та .gitignore оновлені
- [ ] Код протестований локально
- [ ] Git репозиторій ініціалізований
- [ ] Remote origin налаштований

## Після першого успішного build:

1. ✅ Перевір образи в Packages
2. ✅ Перевір Security alerts
3. ✅ Перевір логи workflow
4. ✅ Налаштуй monitoring (опціонально)
5. ✅ Налаштуй автоматичний deployment (опціонально)

## Додаткова допомога:

- Детальна інструкція: `.github/SETUP.md`
- Швидкий старт: `.github/QUICKSTART.md`
- Опис workflow: `.github/workflows/README.md`

